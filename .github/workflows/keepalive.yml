name: Keep Alive

on:
  schedule:
    - cron: "*/15 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping /health
        run: |
          curl -sf "${{ secrets.NOTIFY_URL }}/health" || echo "::warning::Health check failed"
